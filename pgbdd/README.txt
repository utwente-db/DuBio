This directory contains software implementing the 'pgbdd' Postgres extension
with a PGXN Makefile. So to install the software on your local Postgres 
installation just type 'make install' and everyrhing should work automagically.

See file(s) example-dictionary.sql(.log) for an example using the 'dictionary'
type.
